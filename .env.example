# Copy to .env.local and fill in your Spotify app's Client ID.
# The Client ID is not a secret in the PKCE flow, but keep it out of git anyway.
VITE_SPOTIFY_CLIENT_ID=

# Set to 1 to run the UI against local fixtures without a real Spotify login.
VITE_MOCK=
